[workspace]
members = [
    "crates/conduit-core",
    "crates/conduit-derive",
]
exclude = [
    "crates/tauri-plugin-conduit",  # requires system libs (webkit2gtk); built separately in CI
]
resolver = "2"
